import React from 'react'
import CartWidget from '../CartWidget'

export const NavBar = () => {
    return (
        <nav>
            <h1>NavBar</h1>
            <CartWidget />
        </nav>

    );
}

export default NavBar;